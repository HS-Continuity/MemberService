spring:
  application:
    name: memberservice
  config:
    import: "optional:configserver:"
  cloud:
    config:
      uri: http://43.201.146.199:8071
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true


